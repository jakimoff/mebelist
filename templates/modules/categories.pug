.category--wrap
  .container
    .category
      h2.title-global.title_24
        | Категории
      .row
        div(
          v-for="item in items"
          :class="{'col-lg-3 col-md-4 col-sm-6 col-xs-12':!aside, 'col-lg-4':aside}"
          )
          nuxt-link(:to="item.to").category__link
            h5.category__sub.text_18
              | {{ item.title }}
            p.category__products.text_14
              | {{ item.count }}
            Icon(:svg="`arrow-left`")
